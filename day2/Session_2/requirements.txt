# Minimal runtime dependencies for the iris inference service.
# Kept separate from the repo-level requirements.txt so the Docker image
# only installs what the service actually needs (smaller image, faster builds).
fastapi>=0.110
uvicorn[standard]>=0.27
scikit-learn>=1.3
joblib>=1.3
numpy>=1.24
